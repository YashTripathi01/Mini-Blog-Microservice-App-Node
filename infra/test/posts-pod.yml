# POSTS SINGLE POD
apiVersion: v1
kind: Pod
metadata:
    name: posts
spec:
    containers:
        - name: posts
          image: blogs-posts:0.1
          resources:
              limits:
                  memory: "128Mi"
                  cpu: "500m"
